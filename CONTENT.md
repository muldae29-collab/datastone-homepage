# 채워야 할 내용 — 배포 전 체크리스트

사이트 구조와 문구는 완성돼 있다. **남은 건 실제 회사 정보를 넣는 일뿐이다.**

아직 안 채워진 자리는 화면에서 **주황색 점선 밑줄**로 보인다. 전부 찾으려면:

```bash
grep -rn 'class="ph"' *.html
```

> 🔴 **이 파일의 A그룹을 다 채우기 전에는 도메인을 연결하지 말 것.**
> 자리표시자가 보이는 상태로 공개되면 안 하느니만 못하다.

---

## A. 반드시 채워야 배포 가능 (자리표시자 있음)

**남은 건 4개, 자리표시자 4곳이다.** 치명적인 건 없어서 이 상태로 배포해도 무방하다.

| 항목 | 들어가는 곳 | 비고 |
|---|---|---|
| 소프트웨어사업자 신고번호 | about 인증 | |
| 기업신용평가등급 · 평가기관 · 유효기간 | about 인증 | 🔴 재발급 리드타임 2~4주 |
| 직접생산확인증명서 품목 · 유효기간 | about 인증 | |
| 개인정보 보유기간 | contact 동의 문구 | 예: "3개월" |

### ✅ 이미 반영된 것 (2026-08-16)

| 항목 | 값 | 출처 |
|---|---|---|
| 도메인 | `datastone.co.kr` | 사용자 확인 |
| 대표 이메일 | `esa29@datastone.co.kr` | 사용자 확인 |
| 대표전화 | 02-2608-1029 | 사용자 확인 |
| 지하철 | 5호선 발산역 9번 출구 도보 약 8분 | 사용자 확인 (도보 시간은 직선거리 480m 기준 추정) |
| 지도 | 네이버 지도 링크 (naver.me/F42lhDEu) | 사용자 제공 |
| 주차 | 건물 지하주차장 이용 가능 | 사용자 확인 |
| 상호 | 주식회사 데이터스톤 | 사업자등록증 |
| 대표이사 | 이충준 | 사업자등록증 |
| 사업자등록번호 | 737-86-02216 | 사업자등록증 |
| 설립일(개업연월일) | 2021년 12월 20일 | 사업자등록증 |
| 주소 | 서울특별시 강서구 마곡중앙6로 45, A동 6층 601-C10호 (마곡동, 리더스퀘어마곡) | 사업자등록증 |
| 업태 · 종목 | 정보통신업 · 도소매업 / 시스템SW 개발·공급, 응용SW 개발·공급, 전자상거래 | 사업자등록증 |

도메인은 `CNAME` · 각 페이지 `canonical`/`og:url` · `sitemap.xml` · `robots.txt`에 모두 들어갔다.

> ⚠️ **법인등록번호는 일부러 넣지 않았다.** 홈페이지에 공개할 필요가 없는 값이고,
> 등기·금융 절차에 쓰이므로 노출 범위를 좁히는 편이 낫다.
> 필요할 때 사업자등록증 원본에서 옮겨 적을 것.

> 📌 사내 자료(대표 프로필)의 연락처가 개인 메일로 남아 있다면
> **업무용 주소로 고쳐 두는 편이 좋다** — 제안서에 그대로 쓰일 값이다.
> 설립일(2021-12-20)도 함께 채워두면 좋다.

---

## B. 공개 전 반드시 확인할 것 (사실관계)

### 1. 🔴 법인 실적과 대표 개인 이력의 경계
`portfolio.html`은 이 둘을 **의도적으로 분리해 두었다.**

- **법인 설립은 2021년**이다. 그 이전 사업(정부24 암호화 2015, 현대해상 2017,
  행정정보공동이용 2009–2014 등)은 전부 `#ceo` 섹션에 **대표 개인 경력**으로 들어가 있다.
- 이 구분을 지우고 법인 실적으로 올리면 **발주처 자격·실적 검증에서 그대로 문제가 된다.**
- 어느 표에 넣을지는 **실적증명원을 누구 이름으로 발급받을 수 있는가**를 기준으로 판단한다.

### 2. 실적 표기는 "주관기관" 기준이다

실적 표는 **기간 · 사업명 · 주관기관 · 분야** 네 칸이다.
**계약 형태(원수급·하수급·공동수급 지분)와 계약금액은 넣지 않는다.**

- 표에는 사업을 주관한 기관만 적는다. 회사 소개용으로는 이게 표준 표기이고,
  계약 형태를 공개할 의무도 없다.
- 정량 실적(계약기간·금액·지분·실적증명원)이 필요한 발주처에는
  **문의를 받아 개별로 정리해 보낸다.** 그 안내 문구가 실적표 아래에 들어가 있다.
- 주관기관명 표기 자체가 계약상 제한되는 건이 있는지는 한 번 확인해 둘 것.

> 🔴 **계약금액을 표에 넣지 말 것.** 한 번 공개하면 이후 모든 협상의 기준선이 된다.

### 3. 아직 없는 인증은 올리지 않았다
`about.html` 인증 섹션에는 **실제 보유분만** 넣는다.
보유하지 않은 인증은 카드를 만들지 않는다 — 취득한 뒤에 추가한다.

### 4. 사업관리 도구의 표현 수위
`solutions.html`은 이 도구를 **"사내 운영 중 · 외부 판매용은 준비 중"**으로 써 두었다.
아직 판매할 수 있는 상태가 아니므로 "도입 문의"·"시연 신청" 같은 판매 유도 문구는
**일부러 넣지 않았다.** 제품화가 끝난 뒤에 표현을 바꿀 것.

---

### 5. ✅ 로고 — 공식 로고가 들어가 있다 (2026-08-16 완료)

`Datastone.ai` 원본에서 **벡터 그대로** 뽑아 넣었다. 래스터(PNG) 변환이 아니므로
어떤 화면에서도 흐리지 않다.

| 파일 | 용도 |
|---|---|
| `assets/img/logo.svg` | 컬러 가로형 (헤더 등 밝은 배경) |
| `assets/img/logo-light.svg` | 흰색 반전형 (푸터 등 어두운 배경) |
| `assets/img/logo-symbol.svg` | 심볼만 — 워드마크가 안 들어가는 좁은 자리용 |
| `assets/img/favicon.svg` | 탭 아이콘 (심볼 + 네이비 바탕) |

**원본**: 사내 드라이브의 회사 로고 폴더 (`Datastone.ai`)
(확장자는 `.ai`지만 내용은 PDF라 `pdftocairo -svg`로 변환했다. 1페이지=컬러, 2·3페이지=반전형)

#### 원본에서 바꾼 것 — 딱 하나

픽셀 하나하나에 걸려 있던 **미세 그라디언트 41개를 각각 중간색 하나로 평탄화**했다.
2pt 사각형 안의 그라디언트는 웹 크기에서 눈에 보이지 않는데 파일 크기의 90%를 차지했다.

```
98KB → 8KB   (모든 페이지에서 불러오는 파일이라 이 차이가 크다)
```

> 📌 **인쇄물·제안서에는 원본 `.ai`를 쓸 것.** 이 SVG는 웹 전용 최적화본이다.

글자는 윤곽선(path)으로 들어 있어 폰트가 없는 PC에서도 그대로 나온다.

#### 로고에 맞춰 함께 바꾼 것

- **헤더·푸터에서 "데이터스톤 / DataStone" 텍스트를 뺐다.** 로고가 워드마크를 포함한
  가로형(7.63:1)이라 그대로 두면 회사명이 두 번 나온다.
  한글 상호는 푸터 법적 표기(`<dt>상호</dt>`)로 옮겼다.
- **브랜드 색을 로고 실제 색으로 교체했다.** `style.css`의 `:root` 참고.
  로고 색 띠는 `#30a0d2`(맨 위) → `#031f35`(맨 아래)다.

  🔴 **가장 밝은 파랑 `#30a0d2`를 링크·본문 색으로 쓰지 말 것.** 흰 배경 대비가
  2.96:1이라 WCAG AA(4.5:1)에 못 미친다. 링크·버튼은 `#1c668c`(6.31:1)를 쓰고,
  밝은 파랑은 `--brand-bright`로 어두운 배경 위에서만 쓴다.

---

## C. 넣으면 좋은 것 (없어도 배포 가능)

- [ ] **회사 소개 PDF** — `assets/` 에 올리고 about 페이지에서 링크
- [ ] **대표 사진** — about 인사말 옆
- [ ] **사무실 사진** — 오시는 길
- [ ] **OG 이미지** (1200×630) — 카톡·슬랙 링크 미리보기용.
      만든 뒤 각 페이지 `<head>`에 `<meta property="og:image" content="https://도메인/assets/img/og.png">` 추가

---

## D. 작업 방법 메모

### 공지사항 올리기

**`notices.json` 하나만 고친다.** 목록 페이지(`notice.html`)와 전 페이지 상단바의
"최신 공지 한 줄"이 둘 다 이 파일에서 나온다. HTML은 건드리지 않는다.

```json
[
  {
    "date": "2026-09-22",
    "category": "수주",
    "title": "○○기관 ○○시스템 구축사업을 수주했습니다",
    "body": "<p>본문. HTML 태그를 써도 된다.</p>",
    "pinned": false
  },
  { ...기존 공지... }
]
```

| 항목 | 설명 |
|---|---|
| `date` | **`YYYY-MM-DD` 형식 필수.** 이 값으로 정렬한다 |
| `category` | 라벨로 표시 (안내 / 수주 / 채용 / 인증 …). 빼도 된다 |
| `title` | 목록 제목 + 상단바에 나오는 문구 |
| `body` | 본문. HTML 허용 |
| `pinned` | `true`면 날짜와 무관하게 맨 위 + 상단바 고정. 빼면 `false` |

순서는 신경 쓰지 않아도 된다 — 스크립트가 고정 공지 먼저, 그다음 최신순으로 정렬한다.

> 💡 **상단바 문구는 짧게.** 길면 말줄임(…)으로 잘린다. 25자 안팎이 적당하다.

#### 🔴 이 기능의 진짜 비용은 "계속 채우는 것"이다

만드는 건 끝났다. 문제는 **마지막 공지가 1년 전인 게시판은 없느니만 못하다**는 점이다.
발주처는 그걸 "이 회사 아직 돌아가나"로 읽는다.

그래서 규칙을 하나만 정해 둔다:

> **분기에 한 건도 올릴 게 없으면, 공지 기능을 지우는 편이 낫다.**
> 지우는 방법: 각 HTML의 `<div class="topbar" ...>` 블록과 푸터의 공지사항 링크를 빼고,
> `notice.html` · `notices.json`을 삭제한다. 되살리기 쉬우니 부담 없이 판단할 것.

올릴 거리 예시 — 수주·준공, 인증 취득, 채용, 사무실 이전, 휴무 안내.
이 중 **인증 취득은 `about.html` 인증 카드도 함께** 고칠 것.

#### 동작하지 않을 때

- 공지를 못 읽으면 **상단바는 통째로 사라지고**(빈 띠가 남지 않는다) 목록에는
  "등록된 공지사항이 없습니다"가 뜬다. 확인된 동작이다.
- 파일을 브라우저에서 직접 열면(`file://`) 공지가 안 보인다. 보안 정책상 정상이며,
  실제 배포된 사이트에서는 문제없다. 로컬 확인은 `python3 -m http.server`로 할 것.
- JSON 문법이 틀리면(쉼표 하나만 빠져도) 전부 안 나온다. 올리기 전에 확인:

  ```bash
  python3 -m json.tool notices.json
  ```

### 하위 페이지 배경 무늬

`about` · `business` · `solutions` · `portfolio` · `contact` · `notice`의 파란 헤더에는
**로고 심볼이 배경 무늬로 아주 옅게 깔려 있다.** 페이지마다 인상이 다르다.

| 페이지 | 배치 | 의도 |
|---|---|---|
| 회사소개 | 심볼 하나가 오른쪽에 온전히 | 회사가 쌓여온 모양 |
| 사업영역 | 가로로 여러 개 늘어놓음 | 여러 영역을 다룬다 |
| 기술역량 | 크게 확대해 픽셀 몇 칸만 | 안을 들여다보는 느낌 |
| 수행실적 | 바닥에 낮게 깔림 | 연대기처럼 쌓인 실적 |
| 문의하기 | 심볼 위쪽(흩어진 픽셀) | 이제 시작한다 |
| 공지사항 | 심볼 아래쪽(쌓인 층) | 소식이 내려앉는다 |

무늬 파일은 `assets/img/pattern-symbol.svg` **하나뿐**이다(공식 심볼의 단색 흰색 버전).
요청은 한 번만 나가고 페이지별 차이는 `style.css`의 `.page-hero--*::after` 몇 줄로 낸다.

**진하기를 바꾸고 싶다면** 각 규칙의 `opacity`(현재 0.05~0.075)만 고친다.

> 🔴 **0.12를 넘기지 말 것.** 실제로 0.12로 해봤더니 "은근한 배경"이 아니라
> 눈에 띄는 무늬가 되어 제목 가독성을 깎았다. 지금 값은 그래서 내린 것이다.
> 모바일에서는 글이 폭을 다 쓰므로 0.035로 더 낮춘다.

> ⚠️ `background-size`의 %는 헤더 높이(약 300px) 기준이다. **150%를 넘기면
> 심볼이 잘려 로고가 아니라 사각형 몇 개로 보인다.** 확대가 의도인 `solutions`만 예외다.

로고를 교체하면 `pattern-symbol.svg`도 함께 다시 뽑아야 한다 —
`logo-symbol.svg`에서 도형만 가져와 `fill="#ffffff"` 한 겹으로 감싼 파일이다.

### 지도 — 지금은 링크 방식이다

about · contact 두 곳에 **네이버 지도 링크 버튼**(`naver.me/F42lhDEu`)이 들어가 있다.
누르면 위치 확인과 길찾기가 바로 된다.

임베드(지도가 페이지 안에 박히는 것)를 쓰지 않은 이유:
네이버는 Cloud Platform **API 키**가 필요하고(키 관리·사용량 확인이 계속 따라온다),
카카오 퍼가기는 키 없이 되지만 그만큼 페이지가 무거워진다.
**링크 하나로 길찾기까지 이어지므로 유지 부담 대비 효과가 낫다.**

굳이 지도를 박고 싶으면 두 파일의 `<div class="card">` 지도 블록을 통째로 교체한다:

1. 카카오맵(`map.kakao.com`)에서 주소 검색 → **공유 → 지도 퍼가기** → HTML 복사
2. 복사한 `<iframe>` 코드를 그 블록 자리에 붙여넣기
3. 모바일에서 넘치지 않도록 `<iframe>`에 `style="width:100%;border:0"` 를 줄 것

### 문의 폼 연결 (선택)
지금은 **보내기를 누르면 메일 프로그램이 열리는** 방식이다. 서버가 없어도 동작한다.

외부 폼 서비스로 바꾸려면 `contact.html`에서 한 줄만 고치면 된다:

```html
<form data-contact-form data-endpoint="https://formspree.io/f/xxxxxxxx" ...>
```

`data-endpoint`에 주소가 채워지면 스크립트가 자동으로 fetch 전송 방식으로 바뀐다.
(무료 서비스: Formspree, Web3Forms 등. 월 50건 내외까지는 무료인 경우가 많다)

### 도메인 — 이미 반영됨
`datastone.co.kr`이 `CNAME`, 각 페이지 `canonical`·`og:url`, `sitemap.xml`, `robots.txt`에
들어가 있다. **페이지를 새로 만들면 그 페이지에도 세 가지를 잊지 말 것**:

```html
<link rel="canonical" href="https://datastone.co.kr/새페이지.html">
<meta property="og:url" content="https://datastone.co.kr/새페이지.html">
```

그리고 `sitemap.xml`에 `<url><loc>…</loc></url>` 한 줄 추가.

DNS 설정 절차는 `README.md`의 "도메인 연결" 항목에 있다.

### 실적을 추가할 때
`portfolio.html`의 `<tr data-category="...">` 한 줄을 복사해서 늘린다.
`data-category` 값은 위쪽 필터 버튼의 `data-filter` 값과 같아야 한다
(현재 `mydata` / `private`). 새 분류를 만들면 필터 버튼도 함께 추가할 것.

---

## E. 출처

이 사이트의 회사 정보는 **사내 지식 볼트와 사업자등록증**에서 가져왔다.
정확한 파일 위치는 사내 문서(`PORTFOLIO.md`)에 적어 두었다.

**내용이 바뀌면 원본(볼트)을 먼저 고치고, 그다음 이 사이트를 고친다.**
반대로 하면 두 곳이 조용히 갈라진다.
